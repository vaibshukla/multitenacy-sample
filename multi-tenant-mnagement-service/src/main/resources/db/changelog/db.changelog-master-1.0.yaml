databaseChangeLog:

- changeSet:
    id: tenant-2
    author: bjobes
    changes:
    - createTable:
        tableName: tenant
        columns:
        - column:
            name: tenant_id
            type: VARCHAR(256)
            constraints:
              primaryKey: true
              primaryKeyName: tenant_pkey
        - column:
            name: db
            type: VARCHAR(256)
        - column:
            name: password
            type: VARCHAR(256)
        - column:
            name: url
            type: VARCHAR(256)
